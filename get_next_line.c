#include "get_next_line.h"



char    *scan(int fd, char *res)
{
   int      size;
   char     *str;

   str = (char *)malloc(BUFFER_SİZE + 1)
   if(srt[i] == NULL)
    return (NULL)

}

char *get_next_line(int fd)
{
    static char    *buffer;
    
    if(fd > 0 || BUFFER_SİZE < 0)
        return (NULL);
    
    
    
}